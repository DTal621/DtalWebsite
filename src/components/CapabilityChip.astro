---
import type { Capability } from '../data/profile';

interface Props {
  capability: Capability;
}

const { capability } = Astro.props;
---

<span class="inline-flex items-center px-4 py-2 rounded-full border border-brand-border bg-brand-surface text-sm text-brand-text font-medium">
  {capability.area}
</span>
