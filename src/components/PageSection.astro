---
interface Props {
  heading: string;
  /** Omits the top border — use for the first section on a page */
  first?: boolean;
}

const { heading, first = false } = Astro.props;
---

<section
  class:list={[
    'max-w-4xl mx-auto px-4 py-16',
    !first && 'border-t border-brand-border',
  ]}
>
  <h2 class="text-xs font-semibold uppercase tracking-widest text-brand-muted mb-8">
    {heading}
  </h2>
  <slot />
</section>
